# Cloudflare Worker deployment configuration.
name = "is-career-launchpad"
# ES module that exports the Worker's fetch handler.
main = "src/index.js"
# Pins the runtime behavior used when this project was configured.
compatibility_date = "2024-09-01"

# After deploying, set your Groq key as a secret (never put it here):
#   wrangler secret put GROQ_API_KEY
