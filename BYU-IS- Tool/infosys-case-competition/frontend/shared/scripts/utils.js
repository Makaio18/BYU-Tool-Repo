/**
 * Shared browser utilities belong in this file when two or more pages need
 * the same behavior. It intentionally has no runtime logic yet; loading an
 * empty shared script is harmless and preserves a stable extension point.
 */
